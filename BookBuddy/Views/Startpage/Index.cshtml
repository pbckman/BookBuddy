@model StartPageViewModel

@{
    Layout = "~/Views/Shared/Layouts/_ErrorLayout.cshtml";
}

@* <div class="container-sm startpage-wrapper">
    <div class="startpage-header">
        <h1 class="card-title">@Model.CurrentPage.Heading</h1>
    </div>

    <div class="startpage-mainbody">
        @Html.Raw(Model.CurrentPage.MainBody)
    </div>

    
    <component type="typeof(CounterBlazorPage)" render-mode="Server" />
    <component type="typeof(TestForm)" render-mode="Server" />
</div> *@

    @await Component.InvokeAsync("SlideShow")

    <!-- Hero-sektion med bakgrund -->
    <div class="hero-section">
        <div class="content">
            <h1 class="hero-heading">Gör läsning till ett äventyr!</h1>
            <p class="hero-subheading">Quiz för dina favoritböcker – lär och ha kul samtidigt!</p>
            <a class="btn btn-primary btn-lg" href="/signup">Skapa ett konto</a>
            <a class="btn btn-secondary btn-lg" href="/quiz-demo">Prova ett quiz nu</a>
        </div>
    </div>

    <!-- Bildkarusell med Bootstrap -->
    <div id="quizCarousel" class="carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img src="images/quiz1.jpg" class="d-block w-100" alt="Barn med bok" style="height: 500px;">
                <div class="carousel-caption d-none d-md-block">
                    <h5 style="color: #FFF59D;">Upptäck nya äventyr i varje bok!</h5>
                </div>
            </div>
            <div class="carousel-item">
                <img src="images/quiz2.jpg" class="d-block w-100" alt="Belöningar" style="height: 500px;">
                <div class="carousel-caption d-none d-md-block">
                    <h5 style="color: #FFF59D;">Belöningar och medaljer för varje steg!</h5>
                </div>
            </div>
            <div class="carousel-item">
                <img src="images/quiz3.jpg" class="d-block w-100" alt="Quiz och Lärande" style="height: 500px;">
                <div class="carousel-caption d-none d-md-block">
                    <h5 style="color: #FFF59D;">Lär och ha kul tillsammans!</h5>
                </div>
            </div>
        </div>
        <a class="carousel-control-prev" href="#quizCarousel" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#quizCarousel" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>

